import React from 'react';
import ReactDOM from 'react-dom/client';

import FoldingStage from './components/FoldingStage';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <FoldingStage/>
  </React.StrictMode>
);
